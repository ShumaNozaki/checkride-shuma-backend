apiVersion: v1
kind: Service
metadata:
  name: checkride-shuma-b
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 4000
  selector:
    app.kubernetes.io/instance: app-instance
    app.kubernetes.io/name: app
  sessionAffinity: None
  type: ClusterIP