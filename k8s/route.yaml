apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: checkride-shuma-backend
spec:
  tls:
    termination: edge
  to:
    kind: Service
    name: checkride-shuma-backend
    weight: 100
  wildcardPolicy: None